import { StyleSheet } from "react-native";


export const globalStyles = StyleSheet.create({
    activeTabs: {
        backgroundColor: 'red'
    },

    globalMargin: {
        marginHorizontal: 20
    }
})
